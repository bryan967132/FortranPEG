name: FortranPEG

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.Deployment.outputs.page_url }}

    steps:
      - name: Deployment
        id: Deployment
        uses: actions/deploy-pages@v4